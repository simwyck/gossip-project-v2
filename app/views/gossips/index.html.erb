<h1>All Gossips</h1>

<% if @gossips != nil %>
  <% @gossips.each do |gossip| %>

    <h2><%= gossip.anonymous_author %></h2>
    <p><%= gossip.content %></p>
    <p><a href="<%= gossip_path(gossip.id) %>">Show Gossip</a></p>

  <% end %>
<% else %>
  <p>No gossip to display, man. You need to <%= link_to "add your own", new_gossip_path %> NOW!</p>
<% end %>

<p><a href="<%= new_gossip_path %>">Try Gossiping URself</a></p>
